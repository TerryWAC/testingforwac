# Generated by fill-placeholders.sh — pristine snapshots, not source.
.templates/

# The repo root ignores `config.json` (unanchored, for the Deadlock app's
# runtime config). This one is a committed template, so re-include it.
!config.json
